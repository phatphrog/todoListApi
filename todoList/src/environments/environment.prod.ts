//this file is used when we run 'ng serve --environment prod' or 'ng build --environment prod'

export const environment = {
  production: true,

  //URL of production API
  apiUrl: 'http://localhost:3000'
};
